# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Spinify is a Next.js/TypeScript application deployed on Vercel.

## Commands

Once the project is initialized, standard commands will be:

```bash
npm install        # Install dependencies
npm run dev        # Start development server
npm run build      # Production build
npm run lint       # Run ESLint
npm run test       # Run tests (once configured)
```

> Update this file once `package.json` and the source structure are in place.
